ErrorResponseData:
  description: Response containing an error
  content:
    application/json:
      schema:
        $ref: "../schemas/error.yaml#/Error"
      example:
        message: "Bad Request"
